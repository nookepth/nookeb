# Local dev dependencies (Redis for BullMQ)
# Supabase ใช้ hosted project หรือ `supabase start` แยกต่างหาก
services:
  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    volumes:
      - redis-data:/data

volumes:
  redis-data:
